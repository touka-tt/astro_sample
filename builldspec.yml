version: 0.2

phases:
  install:
    commands:
      - npm install
  test:
    commands:
      - npm run astro check
  build:
    commands:
      - npm run build
  deploy:
    commands:
      aws s3 cp dist/ s3://astro-app-tmngtak --recursive